# Supabase local dev state
.branches/
.temp/

# Private seed files (never commit real credentials or PII)
seed_private/
seed_production/

# Local Supabase secrets (generated by CLI — never commit)
.env.local
