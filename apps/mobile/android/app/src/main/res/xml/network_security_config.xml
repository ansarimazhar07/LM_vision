<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for LM-Vision Inspector (development builds).
  Allows cleartext (HTTP) connections to the local backend running on the host machine.
  10.0.2.2 is the Android emulator's alias for the host computer's localhost.
  192.168.0.0/16 and 10.0.0.0/8 cover physical-device USB/WiFi LAN testing.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator host alias -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- USB tethering / LAN physical device testing -->
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="false">10.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
